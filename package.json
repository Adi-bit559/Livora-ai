{
  "name": "livora-ai",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "dev": "npm run dev --prefix frontend"
  }
}
